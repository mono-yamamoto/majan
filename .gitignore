# dependencies
**/node_modules

# build output
dist/

# tooling caches
.playwright-mcp/
.wrangler/
.astro/
.vite/
coverage/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# 実名入りの初期データ（テンプレートは db/seed.sql と db/roster.sql をコミットする / 決定 D-10）
db/seed.local.sql
db/roster.local.sql

# D1 backups (wrangler d1 export)
backups/

# os / editor
.DS_Store
*.log

# Claude Code のローカル設定（マシン固有）
.claude/settings.local.json

# 役一覧の参考画像（QuizKnock の制作物）。
# 事実（役名・翻数・食い下がり）は自作の一覧表に起こしてあるので、
# 画像そのものは公開リポジトリにも公開サイトにも置かない。
MJ_yaku*.jpg
